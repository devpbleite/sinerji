import { StyleSheet } from "react-native";
import params from "../components/params";

const styleMine = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "center",
  },
});

export default styleMine;
